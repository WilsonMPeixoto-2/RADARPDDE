import {
    autoUpdate,
    computePosition,
    flip,
    offset,
    shift,
    size
} from '@floating-ui/dom';

globalThis.FloatingUIDOM = Object.freeze({
    autoUpdate,
    computePosition,
    flip,
    offset,
    shift,
    size
});
