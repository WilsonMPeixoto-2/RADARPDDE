version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "09:00"
      timezone: America/Sao_Paulo
    open-pull-requests-limit: 6
    rebase-strategy: auto
    groups:
      browser-quality:
        patterns:
          - "@axe-core/playwright"
          - "@playwright/test"
      supabase-tooling:
        patterns:
          - "@supabase/supabase-js"
          - "supabase"
      build-and-validation:
        patterns:
          - "acorn"
          - "acorn-walk"
          - "ajv"
          - "esbuild"
          - "http-server"
          - "prettier"
          - "typescript"
    ignore:
      # O runtime homologado permanece Node 24.x. Tipos de uma major futura só entram
      # junto com uma migração deliberada do runtime, não por PR automático.
      - dependency-name: "@types/node"
        update-types:
          - "version-update:semver-major"
      # A 2.116.0 foi homologada e rejeitada por regressão em garantias pgTAP/RLS.
      # O bloqueio é exato: versões futuras (por exemplo 2.116.1/2.117.x) devem voltar
      # a ser propostas e avaliadas normalmente.
      - dependency-name: "supabase"
        versions:
          - "2.116.0"

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "09:30"
      timezone: America/Sao_Paulo
    open-pull-requests-limit: 5
    rebase-strategy: auto
    groups:
      actions:
        patterns:
          - "*"
