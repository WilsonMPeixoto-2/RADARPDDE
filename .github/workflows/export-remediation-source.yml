name: Diagnosticar testes da observabilidade

on:
  pull_request:
    paths:
      - '.github/workflows/export-remediation-source.yml'

permissions:
  contents: read

jobs:
  diagnose-task6-tests:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout do branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Configurar Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Instalar dependências
        run: npm ci

      - name: Aplicar patch
        run: |
          base64 --decode .github/task6-observability.patch.b64 | gzip --decompress > /tmp/task6.patch
          git apply /tmp/task6.patch

      - name: Executar e registrar testes
        id: tests
        shell: bash
        run: |
          set +e
          {
            echo '=== npm run check ==='
            npm run check
            check_status=$?
            echo "CHECK_STATUS=${check_status}"
            echo '=== npm run test:unit ==='
            npm run test:unit
            unit_status=$?
            echo "UNIT_STATUS=${unit_status}"
            echo '=== npm run test:integration ==='
            npm run test:integration
            integration_status=$?
            echo "INTEGRATION_STATUS=${integration_status}"
            test ${check_status} -eq 0 -a ${unit_status} -eq 0 -a ${integration_status} -eq 0
          } > /tmp/task6-tests.log 2>&1
          status=$?
          tail -n 200 /tmp/task6-tests.log
          exit ${status}

      - name: Publicar log completo
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: task6-tests-${{ github.run_id }}
          path: /tmp/task6-tests.log
          if-no-files-found: error
          retention-days: 1
