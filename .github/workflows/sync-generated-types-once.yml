name: Sincronizar tipos gerados uma vez

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Sincronização pontual concluída; workflow pronto para remoção.'
